{
  "name": "tbk-rest",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build:prod",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf dist/",
    "build:prod": "npm run prebuild && tsc --build tsconfig-prod.json",
    "build": "tsc",
    "predoc": "npm run build",
    "pretest": "npm run build && rm db/test.sqlite3; NODE_ENV=test miqro db:migrate; NODE_ENV=test npm run miqro db:seed:all",
    "test": "NODE_ENV=test nyc mocha test/**/*.test.ts",
    "testfile": "npm run pretest && NODE_ENV=test nyc mocha",
    "lint": "eslint ./ --ext ts",
    "fixlint": "eslint ./ --ext ts --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "@miqro/core": "^0.9.28"
  }
}
