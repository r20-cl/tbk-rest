{
  "name": "tbk-rest",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "NODE_ENV=test nyc mocha test/**/*.test.ts",
    "prebuild": "rm -rf lib/",
    "build": "tsc",
    "predoc": "npm run build",
    "pretest": "npm run build && rm db/test.sqlite3; NODE_ENV=test miqro db:migrate; NODE_ENV=test npm run miqro db:seed:all",
    "testfile": "npm run pretest && NODE_ENV=test nyc mocha",
    "lint": "eslint ./ --ext ts",
    "fixlint": "eslint ./ --ext ts --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.17.2",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "@miqro/core": "^1.0.20"
  }
}
